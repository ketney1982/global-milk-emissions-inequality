# Methods Appendix

> *Auto-generated by `methane_portfolio.report`.  Do not edit manually.*

---

## 1. Data Sources

- **Emission Intensity** (species-level): `cercetare-485010.faostat_clean.milk_emission_intensity_2020_2023.csv`
- **Species Structure** (shares): `cercetare-485010.faostat_clean.milk_species_structure.csv`
- **Aggregate Intensity** (country-year): `cercetare-485010.faostat_clean.milk_intensity_country_year.csv`
- Input panel coverage: 182 countries, years 2020-2023, 5 species
- Interval-analysis coverage (Shapley + optimisation): 181 countries
- Countries excluded from interval analyses due to active-share NaN intensity at endpoints: 1

**Note on China deduplication:** FAO reports both "China" (m49=159, aggregate)
and "China, mainland" (m49=156) with near-identical values.  To avoid
double-counting in global totals, the pipeline retains only "China, mainland"
(m49=156) and drops the aggregate entity.

---

## 2. Validation

Three accounting checks are performed:

| Check | Description | Tolerance | Status |
|-------|------------|-----------|--------|
| Share sum | $\sum_s w_{cts} = 1$ | 1e-06 | PASS (727/727) |
| Milk totals | species sum = aggregate | rel_tol = 1e-06 | PASS (727/727) |
| Mixture identity | $I_{ct} = \sum_s w_{cts} \cdot I_{cts}$ | 1e-10 | PASS (727/727) |

Validation report: `outputs/validation_report.csv`

---

## 3. Shapley Decomposition

The change $\Delta I = I_{c,2023} - I_{c,2020}$ is decomposed using **exact two-factor Shapley values**:

$$\Delta_{struct} = \frac{1}{2} \left[ \sum_s (\Delta w_s) I_s^0 + \sum_s (\Delta w_s) I_s^1 \right]$$

$$\Delta_{within} = \frac{1}{2} \left[ \sum_s w_s^0 (\Delta I_s) + \sum_s w_s^1 (\Delta I_s) \right]$$

- **Reconstruction tolerance**: 1.00e-08
- **Global production-weighted structure effect**: -0.004281 kg CO2e/t
- **Global production-weighted within effect**: +0.001850 kg CO2e/t

---

## 4. Bayesian Hierarchical Model

### Specification

$$\log(I_{cts}) \sim \text{StudentT}(\nu, \mu_{cts}, \sigma_s)$$

$$\mu_{cts} = \alpha_s + u_c + \beta_s (t - 2020) + \gamma_s \cdot \mathbf{1}[t \geq 2022]$$

| Parameter | Prior |
|-----------|-------|
| $\alpha_s$ | $\mathcal{N}(\bar y, \sigma_\alpha)$ with $\sigma_\alpha \in [0.5, 2.5]$ |
| $\beta_s$ | $\mathcal{N}(0, 0.5)$ |
| $\gamma_s$ | $\mathcal{N}(0, 0.5)$ |
| $\sigma_s$ | $\text{HalfNormal}(0.5)$ |
| $u_c$ | Non-centered: $u_{c,raw} \sim \mathcal{N}(0,1)$, $u_c = \tau \cdot u_{c,raw}$ |
| $\tau$ | $\text{HalfNormal}(\sigma_\tau)$, $\sigma_\tau = \mathrm{clip}(\mathrm{sd}(\bar y_c), 0.15, 1.0)$ |
| $\nu$ | $\Gamma(6, 1)$ |

### Sampling

- Chains: 4
- Draws: 5000
- Tune: 10000
- Target accept: 0.95
- Seed: 42

### Diagnostics

- Max R-hat: 1.0015533899838627
- Min ESS (bulk): 9718.71167790518
- Min ESS (tail): 22162.616847227488
- Divergences: 0
- Convergence flag (strict, R-hat<1.01, ESS≥400): True
- Convergence flag (relaxed, R-hat<1.1, ESS≥100): True
- Parameters failing strict R-hat: tau



---

## 5. Robust Portfolio Optimisation

$$\min_w \; \lambda \, \mathbb{E}[I'(w)] + (1-\lambda) \, \text{CVaR}_{\alpha}(I'(w))$$

Subject to:
- $\sum_s w_s = 1$, $w_s \geq 0$
- $\frac{1}{2} \| w - w_{ref} \|_1 \leq \delta$
- $w_s = 0$ for species not in current mix (unless expansion allowed)
- **Do-no-harm guard**: optimized mean intensity must not exceed baseline intensity

CVaR is computed via the Rockafellar-Uryasev linear relaxation.

### Default parameters

| Parameter | Value |
|-----------|-------|
| $\lambda$ | 0.5 |
| $\alpha$ | 0.9 |
| $\delta$ | 0.1 |

- `Table4_optimization.csv` is ranked from `robust_optimization_results.csv` by `abs_reduction_mt_ch4` (inventory-scaled, descending) over all analysed countries.
- `sensitivity_grid.csv` is computed on all 181 analysed countries (6516 country-configuration solves over the 4x3x3 grid); `sensitivity_summary_top20.csv` additionally restricts the same grid to the 20 largest producers, for comparability with the R1 release.
- `robust_optimization_results.csv` preserves both export-safe raw (`raw_*`) and final (`optimized_*`) outputs.
- `no_harm_excess_raw` records unconstrained overshoot relative to baseline before guard enforcement.
- Do-no-harm applied to 0 / 181 countries.
- Negative reductions: raw=0, final=0.
- Full traceability file: `outputs/robust_optimization_audit.json`.

---

## 6. Uncertainty Propagation

- **Share uncertainty**: $w \sim \text{Dir}(\kappa \cdot w_{obs})$, $\kappa = 300.0$
- **Intensity uncertainty**: Posterior draws ($n = 500$)
- Combined via joint sampling

---

## 7. Causal Guardrails

> Results are accounting counterfactuals / scenario-based analyses. They do NOT imply causal effects.  No covariates supporting causal identification are present in the data.

All results are labelled as **accounting counterfactuals** (scenario-based portfolio reallocation).
No causal claims are made.

---

## 8. Reproducibility

- RNG seed: 20230101
- PyMC seed: 42
- Run manifest: `outputs/run_manifest.json`

---

*Generated by methane_portfolio v0.1.0.*
